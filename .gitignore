# Répertoires de build CMake
/build/
/build-*/
build/

# Firmware / PlatformIO
firmware/.pio/
firmware/.vscode/

# IDENTIFIANTS Wi-Fi — ne JAMAIS commiter
firmware/src/config.h

# Éditeurs
.vscode/
.idea/
*.user

# Logs & divers
*.log
*.tmp

# OS
.DS_Store
Thumbs.db

# Archive pédagogique (énoncés, indices, corrigés des TP) : gardée en LOCAL
# pour révision, mais hors dépôt.
annexe-tp/

# NB : bench/data/*.csv (données de l'expérience) reste VERSIONNÉ — ce sont
#      les preuves derrière BILAN-BANC.md.
